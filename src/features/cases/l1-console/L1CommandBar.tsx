/**
 * L1CommandBar — Phase 1 stub.
 *
 * Layout-only placeholder for the L1 Case Resolution Console top
 * command bar. Real wiring (status quick-actions, AI commands,
 * navigation hooks) lands in a later phase.
 */
export function L1CommandBar({ caseId: _caseId }: { caseId: string }) {
  return (
    <div className="rounded-md border border-dashed border-slate-300 bg-slate-50 px-4 py-3 text-sm text-slate-500 dark:border-ndark-border dark:bg-ndark-bg/40 dark:text-ndark-muted">
      L1CommandBar — TBD
    </div>
  );
}

export default L1CommandBar;
