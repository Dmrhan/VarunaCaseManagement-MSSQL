/**
 * L1WorkbenchPanel — Phase 1 stub.
 *
 * Layout-only placeholder for the center workbench (notes, files,
 * activity timeline, AI guidance). Real content lands in a later
 * phase; for now we only reserve the space.
 */
export function L1WorkbenchPanel({ caseId: _caseId }: { caseId: string }) {
  return (
    <div className="flex h-full min-h-[240px] items-center justify-center rounded-md border border-dashed border-slate-300 bg-slate-50 text-sm text-slate-500 dark:border-ndark-border dark:bg-ndark-bg/40 dark:text-ndark-muted">
      L1WorkbenchPanel — TBD
    </div>
  );
}

export default L1WorkbenchPanel;
