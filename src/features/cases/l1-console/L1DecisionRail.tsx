/**
 * L1DecisionRail — Phase 1 stub.
 *
 * Layout-only placeholder for the right column (status decisions,
 * customer pulse, watchers, RUNA AI insights). Real wiring lands in
 * a later phase.
 */
export function L1DecisionRail({ caseId: _caseId }: { caseId: string }) {
  return (
    <div className="flex h-full min-h-[240px] items-center justify-center rounded-md border border-dashed border-slate-300 bg-slate-50 text-sm text-slate-500 dark:border-ndark-border dark:bg-ndark-bg/40 dark:text-ndark-muted">
      L1DecisionRail — TBD
    </div>
  );
}

export default L1DecisionRail;
